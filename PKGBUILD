pkgname=mspm
pkgver=0.0.2
pkgrel=0
pkgdesc="A package manager with way too many sources"
arch=('x86_64')
url="https://github.com/AxellaDumbStuff007/mspm"
license=('GPL-3.0-or-later')
depends=('curl' 'yay')
provides=("$pkgname")
sha256sums=('SKIP')

package() {
	sudo mv mspm "/bin"
}
